# better_player_ios

iOS implementation of the [better_player](https://pub.dev/packages/better_player) plugin.

## Usage

This package should not be used directly by app developers. Instead, they should use the main `better_player` package.
