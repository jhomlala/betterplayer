## Unreleased

- Fixed `dart:io` transitive import that broke Web and WASM compatibility.
- Updated `PlayerEngineController` to use `String` paths instead of `File` objects.
- Added `BetterPlayerIoUtils` for platform-agnostic file operations.

## 0.0.122

- Added `BetterPlayerLogger` for advanced logging.
- Fixed aspect ratio issue on some devices.

## 0.0.121

- Initial release.
